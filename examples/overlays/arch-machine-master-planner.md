# Overlay — arch-machine (portable copy for skills/examples)

Copy into a project as `.agents/overlays/arch-machine-master-planner.md` or keep live overlays in the arch-machine repo.

**Live overlays:** in [p10ns11y/arch-machine](https://github.com/p10ns11y/arch-machine) under `.agents/overlays/`.  
**Live ontology:** `.agents/ontology/`.  
**Pack example:** [../master-planner/examples/arch-machine-pack.md](../master-planner/examples/arch-machine-pack.md)

**Thesis:** A true master plan is an interconnected web of friction-removing automations aimed at impossible missions that great vision makes real.

**Pull:**

```bash
# local library, or clone https://github.com/p10ns11y/skills if missing
"$HOME/Work/personal/skills/master-planner/scripts/pull-skills.sh" \
  --project /path/to/arch-machine --pack arch-guardian
# force remote cache:
.../pull-skills.sh --project /path/to/arch-machine --pack arch-guardian --from-remote
```
