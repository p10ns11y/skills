---
name: context-ignite
description: >-
  Fission then fusion: token-efficient context prune followed by synthesis and
  surplus. Optional roadmap handoff.
kind: workflow
skill_chain:
  - ai-optimization
  - fusion-sage
  - stellar-spacemap
---

# context-ignite

1. `ai-optimization` — relevance prune, budgets  
2. `fusion-sage` — synthesis + surplus (Q>1)  
3. optional `stellar-spacemap` — if backlog/doc update requested  

Use for large repos and expensive context, not one-line typos.
