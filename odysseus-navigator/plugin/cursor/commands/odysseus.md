# /odysseus — Navigator (Cursor)

Load and follow **odysseus-navigator**. Prefer `/odysseus-core` for a single bottleneck.

## Do now

1. Name Ithaca.
2. Diagnose all matching mistakes (or `none`). Myth quote ≤1 line each.
3. Waters + spirit (Ithaca always).
4. Hooks only — do not inline control-graph or EVA.
5. If `mistakes=none`: affirm + one next step.

Emit the **Odysseus** table. Permission ask. No yolo. Auth unknown → `eva_hook=Ask`.
