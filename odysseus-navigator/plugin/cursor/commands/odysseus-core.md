# /odysseus-core — faster intelli nail (Cursor)

Load **odysseus-navigator** if attached. Follow this file if not. Do **not** start EVA Prior→Probe. Do **not** open a Control Card unless a hook says so.

## Do now

1. **Ithaca** — one sentence user/business outcome.
2. **Core** — single bottleneck that most advances Ithaca.
3. **At most one** Odysseus mistake (or `none`).
4. Waters; spirit = Ithaca unless the circumstance table matches.
5. **next** — one action.
6. Route: ≥2 emptiness signals → suggest `/eva` (`eva_hook=Ask`); multi-step thrash → suggest control-graph (`cg_hook=ORIENT`); heading math → `/mission-map`; else tiny fix only if `mistakes=none`.
7. Stop. No subagents. No myth essay.

Emit the **Odysseus** table (`ithaca`, `core`, `waters`, `mistakes`, `antidotes`, `spirit`, `why_spirit`, `cg_hook`, `eva_hook`, `next`).

If I pasted a goal after the command, use it. Otherwise ask for one sentence.
